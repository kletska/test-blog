import React from 'react';
import Card from '../UI/Card';
import './style.css';

const Sidebar = (props) => {
    return (
        <Card width="27%">
            sidebar
        </Card>
    );
}

export default Sidebar;